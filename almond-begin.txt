(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        define(['react'], factory);
    } else {
        root.WingspanCursor = factory(root.React);
    }
}(this, function (React) {
