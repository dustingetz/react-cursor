(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        define(['lodash'], factory);
    } else {
        root.WingspanCursor = factory(root._);
    }
}(this, function (_) {
