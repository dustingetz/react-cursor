var Cursor = require('../../src/Cursor');

'use strict';

describe('Cursor', function() {
  it('debug should be set to false', function() {
    expect(Cursor.debug).to.equal(false);
  });

  it('debug should be set to true', function() {
    expect(Cursor.debug).to.equal(false);
  });
});
